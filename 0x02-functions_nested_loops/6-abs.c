#include "main.h"

/**
 * _abs - computes the absolute integer values
 * @n: integer to process
 * Return: integer absolute
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
return (0);
}

