#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints base 16 in lowercase
 * Return: 0 if true, others if not
 */

int main(void)
{
int 0xNew;

for (New = '0'; New <= 'f'; New++)
{
	putchar(New++);
}

putchar('\n');
return (0);
}
