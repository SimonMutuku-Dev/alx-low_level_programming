#include <stdio.h>
#include <stdlib.h>

/**
 * main - Prints all base 10 single digits from 0
 * Return: 0 if true, others if not
 */

int main(void)
{
int n;

for (n = 0; n < 10; n++)
{
printf("%d", n);
}
printf("\n");
return (0);
}
